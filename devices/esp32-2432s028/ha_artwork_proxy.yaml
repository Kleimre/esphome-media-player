# =============================================================================
# HA Artwork Proxy
# =============================================================================
# Resizes album art to a small baseline JPEG for the ESP32 to decode.
#
# 1. Add to configuration.yaml:
#
#    shell_command:
#      resize_album_art: "python3 /config/scripts/resize_art.py '{{ url }}'"
#
# 2. Create /config/scripts/resize_art.py with contents below.
#
# 3. Create automation via HA UI (Settings > Automations):
#    Trigger: state change on media_player.wiim_ma900, attribute: entity_picture
#    Action: call shell_command.resize_album_art with data:
#      url: "http://127.0.0.1:8123{{ state_attr('media_player.wiim_ma900', 'entity_picture') }}"
#
# 4. Requires: pip install Pillow
# =============================================================================
#
# /config/scripts/resize_art.py:
#
# import sys, urllib.request, io
# from PIL import Image
#
# url = sys.argv[1]
# req = urllib.request.Request(url)
# req.add_header("Authorization", "Bearer YOUR_TOKEN_HERE")
# data = urllib.request.urlopen(req, timeout=10).read()
# img = Image.open(io.BytesIO(data))
# img = img.convert("RGB")
# r, g, b = img.split()
# img = Image.merge("RGB", (b, g, r))
# img.thumbnail((160, 160), Image.LANCZOS)
# img.save("/config/www/album_art.jpg", "JPEG", quality=70, progressive=False)
